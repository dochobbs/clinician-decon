# Historical Results

This directory is for regenerated historical comparison outputs.

Raw result JSON files are ignored by git. Keep durable findings in
`historical-fixtures/workbench/docs/` or `docs/qa/`.
