# Reports

This directory is for locally generated validation reports.

JSON reports are ignored by git. Preserve durable findings in `docs/qa/` and regenerate raw
machine-readable reports when needed.
